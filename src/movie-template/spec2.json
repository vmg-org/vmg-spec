{
  "/movie-templates": {
    "post": {
      "operationId": "postMovieTemplate",
      "tags": ["movie-templates"],
      "summary": "Create a template for a movie",
      "description": "Every template",
      "parameters": [{
        "in": "body",
        "name": "body",
        "description": "Data for a movie template",
        "schema": {
          "$ref": "$/definitions/movie_template"
        },
        "required": true
      }],
      "responses": {
        "201": {
          "description": "successful operation",
          "schema": {
            "$ref": "#/definitions/movie_template"
          }
        },
        "400": {
          "description": "Bad request"
        }
      }
    },
    "get": {
      "operationId": "getListOfMovieTemplate",
      "tags": ["movie-templates"],
      "summary": "Get all movie templates",
      "parameters": [],
      "responses": {
        "200": {
          "description": "successful operation",
          "schema": {
            "type": "array",
            "items": {
              "$ref": "#/definitions/movie_template"
            }
          }
        }
      }
    }
  },
  "/movie-templates/{id}": {
    "get": {
      "operationId": "getMovieTemplate",
      "tags": ["movie-templates"],
      "summary": "Get by id",
      "parameters": [{
        "in": "path",
        "name": "id",
        "description": "Id of a movie template",
        "type": "integer",
        "required": true
      }]
    }
  }
}
